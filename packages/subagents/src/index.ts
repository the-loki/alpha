/**
 * The subagent policy: what this workbench can hand work to, what each one may reach for, how long
 * it runs, and the words the model reads. It speaks of subagents and not of agents — running one is
 * `@alpha/internal-plugins`' half, where the agent library may be named (C2.0) — so this is an
 * ordinary library: no pi, no Electron, no window.
 */

export * from './registry.ts'
