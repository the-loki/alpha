/**
 * Auto-retry: coding-agent's policy for a transient provider failure, as a face on the plugin base
 * — the decision the run's end is judged with, and the hook that spends an attempt on it.
 */
export * from './retry.ts'
